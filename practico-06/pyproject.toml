[project]
name = "chromadb-semantic-search-demo"
version = "1.0.0"
description = "Demo de búsqueda semántica con ChromaDB y embeddings"
requires-python = ">=3.10"
dependencies = [
    "chromadb>=0.5.0",
]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
